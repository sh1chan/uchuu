# Uchuu (Universe) database

```
> TCP Server		- (to listen the commands)
> SQLite database	- (to CRUD the data)

Request		>	+------------+
			| TCP Server |
			| +--------+ |
			| | SQLite | |
			| +--------+ |
Response	<	+------------+
```

#### TODO
- [ ] register pages
- [ ] register groups
- [ ] register model
